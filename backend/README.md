# Python Backend

This is the Python backend for the Question Paper CMS application.
It will handle authentication, CRUD operations for classes, templates, questions/answers, database storage (MySQL), and PDF generation.
